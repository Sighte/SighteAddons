#
# https://help.github.com/articles/dealing-with-line-endings/
#
# Linux start script should use lf
/gradlew        text eol=lf

# Same reason, for every other shell script in here
*.sh            text eol=lf

# These are Windows script files and should use crlf
*.bat           text eol=crlf

# The committed build must never be touched by line ending conversion
*.jar           binary

# Same, for the UI sprite sheet. Git's own binary detection already covers it, but this is a
# generated asset whose bytes are compared against tools/gen_ui_sheet.py's output — being explicit
# costs a line and a silent CRLF pass through a PNG costs an afternoon.
*.png           binary

